{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 40,
      "error_msg": "Name must be between 1 and 40 characters long"
    },
    "email": {
      "type": "string",
      "format": "email",
      "error_msg": "Email must be in proper format. Ex: John@gmail.com"
    },
    "password": {
      "type": "string",
      "minLength": 6,
      "error_msg": "Password must be longer than 5 characters"
    }
  },
  "required": ["name", "email", "password"]
}
